{
  "name": "cafeconnect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "generate-prisma": "cd server && npm run prisma:generate",
    "build-client": "cd client && npm install && npx vite build",
    "render-build": "npm run install-all && npm run generate-prisma && npm run build-client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "node server/index.js"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
