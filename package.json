{
  "name": "cafeconnect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "render-build": "npm install --prefix server && npm install --prefix client && npm --prefix server run prisma:generate && npm --prefix client run build",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "node server/index.js"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
